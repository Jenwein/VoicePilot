#pragma once
#include <miniaudio.h>

// TODO:封装miniaudio的相关底层操作
class AudioManager
{
	//例如:
	/*StartRecording(): 开始录制音频到文件。StopRecording(): 停止录制。PlayAudioFile(const std::string& path): 播放指定的音频文件。*/
};

